services:
  db:
    image: postgres:17
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d

#  backend:
#    build: .
#    env_file:
#      - .env
#    ports:
#      - "8080:8080"
#    depends_on:
#      - db
#    restart: "no"

volumes:
  db-data: